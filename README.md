# FastAPI Backend

객체지향 설계 원칙을 따른 FastAPI 백엔드 애플리케이션입니다.

## 📁 프로젝트 구조

```
Backend/
├── app/                                # 메인 애플리케이션 패키지
│   ├── cli/
│   │   └── run_experiment.py           # 실험/백테스트 실행 CLI 스크립트
│   ├── core/
│   │   ├── config.py                   # 설정(환경 변수 로딩, Settings 클래스 등)
│   │   ├── events.py                   # asyncio 기반 실시간 시세 Pub/Sub 이벤트 버스
                                          - KIS WebSocket → 다중 컨슈머 전달
│   │   └── tradingview.py              # TradingView 연동 설정 및 유틸
│   ├── models/                         # 데이터베이스 모델 (SQLAlchemy)
│   │   ├── backtest.py                 # 백테스트 모델
│   │   ├── base.py                     # Base 클래스 (declarative_base 등)
│   │   ├── execution.py                # 체결 모델
│   │   ├── order.py                    # 주문 모델
│   │   ├── paper_trading.py            # 모의투자 모델
│   │   ├── position.py                 # 포지션 모델
│   │   ├── price_data.py               # 시세 데이터 모델
│   │   ├── strategy.py                 # 전략 정보 모델
│   │   ├── ticker.py                   # 종목정보 모델
│   │   ├── trade.py                    # 거래기록 모델
│   │   ├── user.py                     # 사용자 모델
│   │   └── watchlist.py                # 관심종목 모델
│   ├── repositories/                   # 데이터 접근 계층 (DB 쿼리 로직)
│   │   ├── backtest.py                 # 백테스트 데이터 CRUD
│   │   ├── paper_trading.py            # 모의투자 CRUD
│   │   ├── price.py                    # 시세 데이터 CRUD (upsert/쿼리)
│   │   ├── strategy.py                 # 전략 CRUD
│   │   ├── ticker.py                   # 주식 데이터 CRUD
│   │   └── user.py                     # 사용자 CRUD
│   ├── routers/                        # API 라우터
│   │   ├── auth.py                     # 인증/인가 API
│   │   ├── backtest.py                 # 백테스트 실행/조회 API
│   │   ├── paper_trading.py            # 모의투자 주문/정보 API
│   │   ├── price.py                    # 시세 동기화/조회 API
│   │   ├── strategy.py                 # 전략 생성/조회 API
│   │   ├── ticker.py                   # 종목 정보 API
│   │   ├── tradingview.py              # TradingView webhook/연동 API
│   │   ├── user.py                     # 사용자 API
│   │   └── websocket.py                # 실시간 WebSocket API (시세/체결 등)
│   ├── schemas/                        # Pydantic 스키마 (요청/응답 모델)
│   │   ├── auth.py                     # 로그인/토큰 스키마
│   │   ├── backtest.py                 # 백테스트 요청/응답 스키마
│   │   ├── price.py                    # 시세 데이터 스키마
│   │   ├── strategy.py                 # 전략 스키마
│   │   ├── ticker.py                   # 종목 데이터 스키마
│   │   ├── tradingview.py              # TradingView webhook 스키마
│   │   └── user.py                     # 사용자 스키마
│   ├── services/                       # 비즈니스 로직 계층
│   │   ├── auth.py                     # 인증 로직 (패스워드/토큰 처리)
│   │   ├── backtest.py                 # 백테스트 실행/관리 서비스
│   │   ├── kis_auth.py                 # KIS 인증 매니저 (토큰 획득/갱신)
│   │   ├── kis_prices.py               # KIS 시세 조회 클라이언트
│   │   ├── kis_websocket.py            # KIS WebSocket 실시간 시세 처리
│   │   ├── order_executor.py           # 주문 실행 엔진 서비스
│   │   ├── paper_trading.py            # 모의투자 처리 서비스
│   │   ├── price.py                    # 시세 수집/저장 서비스
│   │   ├── strategy.py                 # 전략 서비스 (생성/평가 등)
│   │   ├── ticker.py                   # 종목 데이터 서비스
│   │   ├── tv_history.py               # TradingView 히스토리 데이터 fetcher
│   │   ├── tv_symbol.py                # TradingView 심볼 변환/매핑 로직
│   │   └── user.py                     # 사용자 서비스
│   ├── utils/                          # 유틸리티 모음
│   │   ├── datetime.py                 # 날짜/시간 유틸
│   │   ├── dependencies.py             # DI 의존성 (get_current_user 등)
│   │   ├── indicator_calculator.py     # 기술적 지표 계산 유틸
│   │   ├── kpi_calc.py                 # 전략/수익률 KPI 계산기
│   │   ├── llm_client.py               # LLM 연동 유틸 (Gemini 등)
│   │   ├── logger.py                   # 로깅 설정/헬퍼
│   │   ├── mst_parser.py               # MST 파일 파서(종목 코드 데이터)
│   │   ├── resample.py                 # 캔들/봉 데이터 리샘플링 유틸
│   │   ├── router.py                   # 라우터 공통 유틸
│   │   ├── security.py                 # JWT/암호 해시/보안 유틸
│   │   ├── seed_data.py                # 초기 데이터 시딩 유틸
│   │   ├── strategy.json               # 기본 전략 설정 JSON
│   │   ├── timezone.py                 # 타임존 변환 유틸
│   │   └── tv_format.py                # TradingView 포맷 변환 유틸
│   ├── database.py                     # DB 연결/세션 관리
│   └── main.py                         # FastAPI 앱 진입점 (라우터 등록)
├── mnt/
│   └── data/                           # 정적 주식 데이터
│       ├── konex_code.mst.zip         
│       ├── kosdaq_code.mst.zip         
│       ├── kospi_code.mst.zip          
│       ├── nxt_kosdaq_code.mst.zip   
│       └── nxt_kospi_code.mst.zip    
```

## 🚀 빠른 시작

### 1. 환경 설정

```bash
# 환경 변수 설정
cp .env.example .env
# .env 파일을 편집하여 설정값을 조정하세요
```

### 2. 의존성 설치

```bash
# Python 가상환경 생성 (권장)
python -m venv venv
source venv/bin/activate  # Linux/Mac
# venv\Scripts\activate   # Windows

# 의존성 설치
pip install -r requirements.txt

# 또는 개발 의존성 포함 설치
pip install -e ".[dev]"
```

### 3. 데이터베이스 초기화 및 데이터 시딩

```bash
# 애플리케이션을 처음 실행하면 자동으로 초기 데이터가 시딩됩니다.
# 기본 전략 5개가 자동으로 생성됩니다:
# - 이동평균 교차 전략
# - RSI 과매수/과매도 전략
# - 볼린저 밴드 돌파 전략
# - MACD 시그널 전략
# - 스토캐스틱 전략

# 수동으로 데이터를 시딩하려면:
python -m app.scripts.seed
```

### 4. 애플리케이션 실행

```bash
# 개발 서버 실행
make run
# 또는
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### 5. Docker로 실행

```bash
# Docker Compose로 전체 스택 실행
docker-compose up --build

# 또는 Docker만 사용
docker build -t fastapi-backend .
docker run -p 8000:8000 fastapi-backend
```

## 📖 API 문서

서버 실행 후 다음 URL에서 API 문서를 확인할 수 있습니다:

- **Swagger UI**: http://localhost:8000/caps_lock/api/docs
- **ReDoc**: http://localhost:8000/redoc

- **최종 구현까지 Swagger UI JWT 인증 생략하는 방법 -> .env 파일에 'SKIP_AUTH=true' 한 줄 추가**

## 🏗️ 아키텍처

이 프로젝트는 **레이어드 아키텍처(Layered Architecture)** 패턴을 따릅니다:

1. **Controller Layer**: API 엔드포인트와 요청/응답 처리
2. **Service Layer**: 비즈니스 로직 처리
3. **Repository Layer**: 데이터 접근 추상화
4. **Model Layer**: 데이터 모델 정의

### 주요 설계 원칙

- **의존성 역전 원칙**: 상위 계층이 하위 계층에 의존하지 않도록 추상화 사용
- **단일 책임 원칙**: 각 클래스와 모듈은 하나의 책임만 가짐
- **개방-폐쇄 원칙**: 확장에는 열려있고 수정에는 닫혀있는 구조
- **인터페이스 분리 원칙**: 클라이언트가 사용하지 않는 인터페이스에 의존하지 않음

## 🛠️ 개발 도구

### 코드 품질

```bash
# 코드 포맷팅
make format

# 린팅
make lint

# 테스트
make test

# 커버리지 테스트
make test-cov
```

### 유용한 명령어

```bash
# 도움말 보기
make help

# 개발 환경 설정
make dev-install

# 프로덕션 서버 실행
make run-prod

# 캐시 정리
make clean
```

## 🔧 환경 변수

주요 환경 변수들:

| 변수명         | 설명              | 기본값                 |
| -------------- | ----------------- | ---------------------- |
| `APP_NAME`     | 애플리케이션 이름 | `FastAPI Backend`      |
| `ENVIRONMENT`  | 실행 환경         | `development`          |
| `DATABASE_URL` | 데이터베이스 URL  | `sqlite:///./app.db`   |
| `SECRET_KEY`   | JWT 비밀 키       | `your-secret-key-here` |
| `LOG_LEVEL`    | 로그 레벨         | `INFO`                 |

자세한 설정은 `.env.example` 파일을 참조하세요.

## 📝 개발 가이드

### 새로운 기능 추가

1. **모델 정의**: `app/models/`에 SQLAlchemy 모델 추가
2. **스키마 정의**: `app/schemas/`에 Pydantic 스키마 추가
3. **Repository 생성**: `app/repositories/`에 데이터 접근 로직 추가
4. **Service 생성**: `app/services/`에 비즈니스 로직 추가
5. **Controller 생성**: `app/controllers/`에 API 엔드포인트 추가
6. **라우터 등록**: `app/main.py`에 라우터 추가

### 테스트 작성

```python
# tests/test_example.py
import pytest
from fastapi.testclient import TestClient
from app.main import app

client = TestClient(app)

def test_health_check():
    response = client.get("/api/v1/health")
    assert response.status_code == 200
    assert response.json()["status"] == "healthy"
```

## 🤝 기여하기

1. 프로젝트를 포크합니다
2. 기능 브랜치를 생성합니다 (`git checkout -b feature/AmazingFeature`)
3. 변경사항을 커밋합니다 (`git commit -m 'Add some AmazingFeature'`)
4. 브랜치에 푸시합니다 (`git push origin feature/AmazingFeature`)
5. Pull Request를 생성합니다

## 📄 라이선스

이 프로젝트는 MIT 라이선스 하에 있습니다. 자세한 내용은 `LICENSE` 파일을 참조하세요.
